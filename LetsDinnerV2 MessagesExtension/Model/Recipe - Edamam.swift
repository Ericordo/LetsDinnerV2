//
//  Recipe.swift
//  LetsDinnerV2 MessagesExtension
//
//  Created by Eric Ordonneau on 05/11/2019.
//  Copyright © 2019 Eric Ordonneau. All rights reserved.
//

import Foundation

/*
struct Recipe {
    
    var title: String?
    var imageUrl: String?
    var uri: String?
    var sourceUrl: String?
    var ingredientList: [String]?
    
    init() {}
    
    init(title: String, sourceUrl: String) {
        self.sourceUrl = sourceUrl
        self.title = title
    }
    
    init(dict: Dictionary<String, Any>) {
        if let recipeDict = dict["recipe"] as? Dictionary<String, Any> {
            if let label = recipeDict["label"] as? String {
                self.title = label
            }
            if let imageUrl = recipeDict["image"] as? String {
                self.imageUrl = imageUrl
            }
            if let uri = recipeDict["uri"] as? String {
                self.uri = uri
            }
            if let ingredientLines = recipeDict["ingredientLines"] as? [String] {
                self.ingredientList = ingredientLines
            }
            if let sourceUrl = recipeDict["url"] as? String {
                self.sourceUrl = sourceUrl
            }
        }
    }
}
 */
